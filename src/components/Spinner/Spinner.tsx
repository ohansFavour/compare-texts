import React from 'react'

// styles
import "./Spinner.scss"

const Spinner = () => {
    return (
        <div className="spinner">
            
        </div>
    )
}

export default Spinner
